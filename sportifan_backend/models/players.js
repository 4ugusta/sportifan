class Player {
    constructor(id,name, role, battingStyle, bowlingStyle,
         status ) {
            this.id = id;
            this.name = name
            this.role = role
            this.battingStyle =battingStyle
            this.bowlingStyle = bowlingStyle
            this.status = status;
    }
}

module.exports = Player;